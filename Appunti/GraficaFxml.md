# Grafica Fxml
### Mettere bottone CHECKMARK
In `Scen Builder` creare un `OK Button`, nel menù `Properties` selezionare al tab `Text` il simbolo **square root** e nello `Style` come in figura: 
![Logo](img/checkBox.png)
